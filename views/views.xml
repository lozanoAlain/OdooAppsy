<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="appsy.list">
      <field name="name">appsy list</field>
      <field name="model">appsy.appsy</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="appsy.action_window">
      <field name="name">appsy window</field>
      <field name="res_model">appsy.appsy</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="appsy.action_server">
      <field name="name">appsy server</field>
      <field name="model_id" ref="model_appsy_appsy"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="appsy" id="appsy.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="appsy.menu_1" parent="appsy.menu_root"/>
    <menuitem name="Menu 2" id="appsy.menu_2" parent="appsy.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="appsy.menu_1_list" parent="appsy.menu_1"
              action="appsy.action_window"/>
    <menuitem name="Server to list" id="appsy" parent="appsy.menu_2"
              action="appsy.action_server"/>
    -->
  </data>
</odoo>